spring:
  application:
    name: team5_config_server

server:
  port: 8888

eureka:
  client:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    prefer-ip-address: true
