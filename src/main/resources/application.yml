server:
  port: 8888

spring:
  application:
    name: config-sever

  cloud:
    config:
      server:
        git:
          url:

# --- ??? Eureka Server? ???? ?? ?? ---
# eureka:
#   client:
#     service-url:
#       defaultZone: http://localhost:8761/eureka/